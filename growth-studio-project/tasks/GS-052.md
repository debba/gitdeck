# GS-052 — Snapshot attribution at 48 hours and 7 days

**Phase:** Phase 5 — Measure and learn

- Create `src/utils/growth/attribution.ts` and `tests/utils/growth/attribution.test.ts` with pure helpers that select D-046 snapshot boundaries and calculate signed star and fork deltas for `48h` and `7d`. Inject the current time, use UTC snapshot dates, enforce the one-day boundary tolerance, and report a window as pending when publication or either usable snapshot is absent.
- Create `src/server/growth/attribution.ts` and `tests/server/growthAttribution.test.ts`. Reuse `getRepositorySnapshotHistory` and account-scoped published content; never read snapshots in the browser. Measure only due windows, preserve pending work, and make refreshes deterministic and idempotent.
- Extend `src/types/growth.ts`, `src/server/growth/store.ts`, `tests/server/growthStore.test.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with account-scoped `upsertContentPerformance`, `listContentPerformance`, typed performance filters and envelopes, `GET /api/growth/performance` filters `repo`, `contentId`, and `window`, and allowlisted `POST /api/growth/performance/refresh` body `{ repository? }`. Reject duplicate or unknown filters, validate optional repositories and windows, return 404 semantics when an explicitly requested content ID is not owned by the active account, and reuse the existing `content_performance` table without a schema replacement.
- Test exact and jittered boundaries, negative deltas, missing and stale snapshots, not-yet-due windows, both windows becoming due, update-on-refresh behavior, repository filtering, account isolation, malformed bodies and filters, and content deletion cascade.

Acceptance:

- Due published content receives at most one current account-scoped 48-hour and 7-day performance row from bounded daily star and fork snapshots, while unsupported or incomplete history remains explicitly pending.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
