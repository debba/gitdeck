# GS-063 — Growth-wide settings persistence and API

**Phase:** Phase 6 — Unified and release

- Create `src/utils/growth/settings.ts` and `tests/utils/growth/settings.test.ts` with pure defaults and exact-document normalization for growth-wide `timezone`, complete `GrowthCadence`, and non-empty `GrowthPillar[]`. Reuse the constraints and stable normalization already established by `normalizeGrowthProfileInput`: valid IANA timezone, every supported channel with integer cadence from zero through 14, unique slug pillar IDs, integer weights from zero through 100, and at least one positive pillar.
- Create `src/server/growth/settings.ts` and `tests/server/growthSettings.test.ts`. Persist one account-scoped document through `getPreference`, `setPreference`, and `deletePreference` using scope `growth` and an account-qualified key; reads return copied built-in values without inserting, writes round-trip normalized values, reset deletes only the active account's override, and corrupt stored JSON fails closed to defaults without affecting profiles.
- Extend `src/types/growth.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with typed `GrowthSettings`, `GET /api/growth/settings`, complete-document `PUT /api/growth/settings`, and empty-body `DELETE /api/growth/settings`. Require the active account, reject unknown, missing, malformed, and duplicate inputs consistently with existing Growth routes, and return `{ ok: true, settings }` for read, save, and reset.
- Test built-in defaults, defensive copies, every validation boundary, save and reset, corrupt persistence, two-account isolation, anonymous responses, client errors and abort signals, no `growth_profiles` mutation, locale-independent stored data, idempotent existing preference schema use, and no new native dependency.

Acceptance:

- Each account has a validated, resettable Growth Studio default cadence, pillar set, and timezone contract without a new table or any mutation of repository profiles.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Record the account-qualified preference key in `docs/GS_DECISIONS.md`. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
