# GS-047 — Phase 4 closure and phase 5 authoring

**Phase:** Phase 4 — Media

- Verify every phase 4 feature row in `docs/GS_FEATURE_MATRIX.md` is `DONE` with accurate shipped locations. Review uploads, account-scoped local and remote serving, README/website imports, all five SVG cards, Library previews, drawer attachment/removal, Queue and drawer clipboard/download behavior, and the protected-status media gate together at 1440, 1024, and 390 pixels in dark and light themes; fix only phase 4 regressions.
- Confirm all phase 4 `growth.*` keys exist in both locales, all asset/card/media controls are keyboard reachable, Escape still closes `ContentItemDrawer`, account switches abort stale asset requests, SVG text is escaped, every user-supplied server URL uses the shared SSRF guard, uploads and proxies are bounded, and no code rasterizes on the server or posts to a social network. Confirm `package.json` has no new native dependency and run the standard validation gate after any repair.
- Author phase 5 task files `GS-050` through `GS-057` and append matching `PENDING` rows to `PROGRESS.md`: pure opportunity rules plus account-scoped rule scanning (GS-050); opportunity scanning UI in the Interventions backlog (GS-051); 48-hour and 7-day snapshot attribution with performance persistence (GS-052); channel and pillar performance summaries (GS-053); repository and global weekly Growth Review with optional AI narrative (GS-054); evergreen recycling into deduplicated ideas (GS-055); bounded performance-based pillar re-weighting during planning (GS-056); phase 5 closure and phase 6 authoring (GS-057).
- Each authored task must follow the existing task structure, name exact files to create or modify, place pure logic in `src/utils/growth` with mirrored tests, and reference only helpers shipped through GS-046 or created by an earlier phase 5 task. Keep forge and snapshot reads server-side, make AI narrative optional with deterministic fallback, preserve reviewed/published content, and record any new decision in `docs/GS_DECISIONS.md` before relying on it.

Acceptance:

- Phase 4 works end to end without account-scope, path, SSRF, upload-bound, SVG-injection, keyboard, responsive, locale, rasterization, media-gate, or outbound-posting regressions; phase 5 tasks and pending ledger rows are specific and consistent with shipped contracts.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
