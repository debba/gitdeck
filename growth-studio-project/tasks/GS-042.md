# GS-042 — README and website media imports

**Phase:** Phase 4 — Media

- Create `src/utils/growth/importCandidates.ts` and `tests/utils/growth/importCandidates.test.ts`. Extend `src/types/growth.ts`, `src/server/growth/assets.ts`, `src/server/growth/signals.ts`, `src/server/growth/store.ts`, `src/server/routes/growth.ts`, `src/api/growth.ts`, `src/components/growth/GrowthAssetLibrary.tsx`, `tests/server/growthAssets.test.ts`, `tests/server/growthRoutes.test.ts`, and `tests/components/growth/GrowthAssetLibrary.test.ts` with repository README and configured website/repository source imports.
- Add `GET /api/growth/assets/import-candidates?repo=owner/name` and JSON `POST /api/growth/assets/import` with allowlisted `{ repository, origin, url, title, alt }`. Candidate discovery must reuse `fetchReadmeSignal`, `fetchAdditionalSourceSignals`, and the pure normalizer; imports accept only an HTTP or HTTPS media URL from the current candidate set, deduplicate normalized URLs per account and repository, and persist `origin='readme'` or `origin='website'` with no local path.
- Export and reuse a bounded public-media reader from `src/server/growth/signals.ts`. Validate DNS and every redirect with the existing SSRF rules, require an allowlisted image/video response type, enforce the GS-040 byte limit, and reject credentials, private addresses, redirect escapes, HTML, and unavailable media. Extend the authenticated asset file route to proxy URL-backed assets through that reader on every request; never make a browser-provided URL request through an unguarded server fetch.
- Add a localized import section to the asset Library with candidate refresh, source/origin labels, previews through the authenticated file URL after import, required editable alt text, per-item busy/error states, and duplicate feedback. Abort discovery and imports when account or repository changes.
- Tests cover relative and duplicate URL normalization, README and configured-source origins, account scope, forged URLs, private IPv4/IPv6 and redirect targets, bounded media reads, content-type rejection, idempotent imports, proxy headers, stale UI requests, and empty/error states.

Acceptance:

- A maintainer can discover and save verified README or source-library media as repository assets; every server-side website/media request remains SSRF-guarded and bounded, and imported bytes are exposed only through an authenticated account-scoped proxy.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
