# GS-056 — Bounded performance-based pillar re-weighting

**Phase:** Phase 5 — Measure and learn

- Create `src/utils/growth/performanceWeights.ts` and `tests/utils/growth/performanceWeights.test.ts` with a pure D-049 re-weighting function. Use only complete `7d` measurements from the eight weeks before the plan, require at least three items across two positive-weight pillars, score signed star and fork deltas per measured item, clamp each multiplier from 0.8 through 1.2, keep zero weights at zero, and deterministically redistribute integer rounding so the configured positive total is preserved.
- Extend `src/server/growth/planner.ts`, `tests/server/growthPlanner.test.ts`, `src/server/growth/performance.ts`, and `tests/server/growthPerformance.test.ts` so initial and regenerated plans read only the active account and repository's eligible prior measurements before slot construction. Feed adjusted pillars to `buildGrowthPlanSlots`, persist them only in the content-plan snapshot, and leave `growth_profiles` unchanged. No data, insufficient samples, unknown pillars, or unusable scores must retain configured weights exactly.
- Extend `src/types/growth.ts`, `src/api/growth.ts`, `src/components/growth/calendar/GrowthPlanManager.tsx`, `tests/components/growth/GrowthPlanManager.test.ts`, `src/i18n/en.ts`, and `src/i18n/it.ts` with a typed `weightsAdjusted` generation result and a localized, non-blocking plan notice. Keep existing AI assignment fallback, active-plan overlap checks, and response roles intact.
- Test deterministic bounds and rounding, negative and outlier performance, minimum samples, eight-week exclusion edges, account and repository isolation, profile immutability, adjusted slot distribution, API response compatibility, and regeneration preserving ready, scheduled, published, and skipped content.

Acceptance:

- A sufficiently measured repository biases its next plan toward better-performing pillars within strict bounds, while sparse data, profiles, reviewed work, publication history, and existing planner behavior remain unchanged.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
