# GS-057 — Phase 5 closure and phase 6 authoring

**Phase:** Phase 5 — Measure and learn

- Verify every phase 5 feature row in `docs/GS_FEATURE_MATRIX.md` is `DONE` with accurate shipped locations. Review opportunity scanning, 48-hour and 7-day attribution, channel and pillar summaries, repository and global Growth Review, evergreen recycling, and bounded plan re-weighting together at 1440, 1024, and 390 pixels in dark and light themes; fix only phase 5 regressions.
- Confirm deterministic rules and review output at threshold boundaries, account and repository scope for every store and endpoint, server-only forge and snapshot reads, status preservation on rescans and recycling, published-content immutability, profile immutability during re-weighting, stale-request cancellation, keyboard access, locale parity, and AI-independent fallback. Run the standard validation gate after any repair and confirm no outbound social posting or new native dependency.
- Inventory the contracts actually shipped through GS-056, record deviations in `docs/GS_DECISIONS.md`, then author phase 6 task files `GS-060` through `GS-067` and matching `PENDING` rows in `PROGRESS.md`: unified calendar read model and API (GS-060); unified month, week, and filters UI (GS-061); multi-repository generation and pillar deconfliction (GS-062); growth-wide settings persistence and API (GS-063); settings UI and default application (GS-064); global home and review release integration (GS-065); README, CHANGELOG, screenshots, and upgrade documentation (GS-066); final responsive, accessibility, security, migration, and release QA (GS-067).
- Each authored task must use exact shipped names, put pure logic in `src/utils/growth` with mirrored tests, keep forge access server-side, preserve per-repository calendar routes, and make one independently verifiable vertical slice. Do not implement phase 6 source work in this task.

Acceptance:

- Phase 5 works end to end without metric, account-scope, status, profile, AI-fallback, keyboard, locale, or responsive regressions; phase 6 tasks and pending ledger rows are specific and consistent with the shipped learning-loop contracts.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
