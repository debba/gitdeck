# GS-055 — Evergreen recycling into deduplicated ideas

**Phase:** Phase 5 — Measure and learn

- Create `src/utils/growth/evergreen.ts` and `tests/utils/growth/evergreen.test.ts` with clock-injected eligibility, stable `evergreen:<contentId>` identity, and D-048 clone-field decisions. Only published items at least 60 days old with `evergreen=1` qualify; malformed dates, non-published content, and boundary-early requests fail closed.
- Create `src/server/growth/recycling.ts` and `tests/server/growthRecycling.test.ts`. Extend `src/server/growth/store.ts` and `tests/server/growthStore.test.ts` with one transaction that resolves the account-owned evergreen rule intervention from GS-050 and creates at most one linked `idea`. Preserve the source row and reviewed or published content exactly, carry repository, channel, format, pillar, goals and evidence URLs, and clear plan, schedule, draft copy, media and evergreen state as required by D-048.
- Extend `src/types/growth.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with empty-body `POST /api/growth/interventions/:id/recycle`. Reject non-rule, non-evergreen, stale, missing, and cross-account interventions without disclosing foreign IDs; repeated requests return the existing idea with an explicit `duplicate` flag.
- Extend `src/components/growth/GrowthInterventions.tsx`, `src/components/growth/ContentItemDrawer.tsx`, `tests/components/growth/GrowthInterventions.test.ts`, `tests/components/growth/ContentItemDrawer.test.ts`, `src/styles/growth/interventions.css`, `src/styles/growth/content-drawer.css`, `src/i18n/en.ts`, and `src/i18n/it.ts`. Add an editable Evergreen control to the drawer and a keyboard-reachable Recycle to idea action only on eligible evergreen rule cards; reconcile returned content, preserve normal drafting, and show busy, duplicate, success, and inline error states.

Acceptance:

- A published item deliberately marked evergreen can surface after 60 days and create one fresh linked idea without overwriting source copy, bypassing drafting or media review, or multiplying ideas on retries.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
