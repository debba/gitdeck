# GS-053 — Channel and pillar performance summaries

**Phase:** Phase 5 — Measure and learn

- Create `src/utils/growth/performanceSummary.ts` and `tests/utils/growth/performanceSummary.test.ts` with pure aggregation of published content and GS-052 performance rows into separate `48h` and `7d` totals, stable channel groups, and stable pillar groups. Count each content item once per window, retain signed metric totals, include measured-item counts, and use a localized-display-safe fallback key for content without a pillar.
- Create `src/server/growth/performance.ts` and `tests/server/growthPerformance.test.ts`. Join only account-owned content and performance, support an optional repository plus inclusive published-at range, and return zero-valued summaries when no measurements exist; do not trigger attribution or forge reads from a summary request.
- Extend `src/types/growth.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with typed summary entities and `GET /api/growth/performance/summary?repo=owner/name&from=<UTC ISO>&to=<UTC ISO>`. Reject duplicate, unknown, malformed, or reversed filters and keep repository filtering account-scoped.
- Test mixed channels and pillars, both windows, negative and absent metrics, unmeasured published items, date edges, deterministic ordering, repository isolation, cross-account rows, empty data, and route/client validation. This task adds the reusable summary contract; the Review UI is owned by GS-054.

Acceptance:

- Clients can request reproducible account-scoped 48-hour and 7-day performance totals grouped by channel and pillar without double-counting windows or causing upstream network work.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
