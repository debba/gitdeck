# GS-035 — This-week editorial queue

**Phase:** Phase 3 — Editorial plan

- Create `src/components/growth/calendar/GrowthQueue.tsx` and `tests/components/growth/GrowthQueue.test.ts`. Add a Queue tab to `GrowthCalendar` at the existing repository calendar route using `?view=queue&date=YYYY-MM-DD`; do not add a second workspace route or sidebar item.
- Derive the Monday-through-Sunday range in the profile timezone with `src/utils/growth/calendar.ts`. List this week's non-skipped items that have `scheduledFor`, ordered by local time, and group them into needs draft, draft, ready, scheduled, and published sections. Include channel, pillar, media-completeness, and scheduled time, with localized loading, empty, and error states.
- Provide inline copy text and copy X thread actions, reusing `formatXThreadForCopy`; show existing media previews and source links, but leave binary image clipboard and generated-card download behavior to phase 4. Open `ContentItemDrawer` for editing or drafting details.
- Add an optional published URL input and mark-published action using `markGrowthContentPublished`. Update queue state with the response and show the existing mandatory-media error inline rather than removing or posting the item. The app must never send content to a social network.
- Extend `src/styles/growth/calendar.css`, `src/i18n/en.ts`, and `src/i18n/it.ts`. Tests cover timezone week boundaries, copy behavior, publication success, media-gate failure, drawer updates, and stale account request cancellation.

Acceptance:

- A maintainer can work through the current repository's dated items for a selected week, copy publishable text, and record manual publication without outbound posting or a media-gate bypass.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
