# GS-038 — Phase 3 closure and phase 4 authoring

**Phase:** Phase 3 — Editorial plan

- Verify every phase 3 feature row in `docs/GS_FEATURE_MATRIX.md` is `DONE` with accurate shipped locations. Review plan generation, drafting, calendar month/week/queue, rescheduling, publication recording, plan lifecycle, and ICS export together at 1440, 1024, and 390 pixels in dark and light themes; fix only phase 3 regressions.
- Confirm all new `growth.*` keys exist in both locales, all calendar items and plan actions are keyboard reachable, Escape closes `ContentItemDrawer`, account switches abort stale data, and no code performs outbound social posting. Run the standard validation gate after any repair.
- Author phase 4 task files `GS-040` through `GS-047` and append their `PENDING` rows to `PROGRESS.md`: account-scoped asset store/API and safe file serving (GS-040); Library asset list and uploads (GS-041); README and SSRF-guarded website media imports (GS-042); server SVG card templates and endpoint (GS-043); browser canvas rasterization with pure helpers and tests (GS-044); image clipboard copy with download fallback in Queue and drawer (GS-045); media attachment/removal UI and complete ready-state gate coverage (GS-046); phase 4 closure and phase 5 authoring (GS-047).
- Each authored task must follow the existing task structure, name exact files to create or modify, preserve client-side rasterization and the no-native-dependency rule, and reference only helpers shipped through GS-037 or created by an earlier phase 4 task. Record any new decision in `docs/GS_DECISIONS.md` before relying on it.

Acceptance:

- Phase 3 works end to end without layout, keyboard, account-scope, locale, media-rule, or outbound-posting regressions; phase 4 tasks and matching pending ledger rows are specific and consistent with shipped contracts.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
