# GS-064 — Growth settings UI and repository defaults

**Phase:** Phase 6 — Unified and release

- Create `src/components/growth/GrowthSettings.tsx` and `tests/components/growth/GrowthSettings.test.ts`, replace the `/growth/settings` placeholder in `src/components/growth/GrowthStudioApp.tsx`, and use the GS-063 fetch, update, and reset helpers. Follow the existing `GrowthLibrary` profile form patterns for timezone, cadence, and ordered pillars without duplicating validation logic; include loading, saved, reset-confirmation, validation, and request-error states plus a new-window `rel="noopener"` link to `/preferences#preferences-ai`.
- Extend `src/utils/growth/profileDefaults.ts`, `tests/utils/growth/profileDefaults.test.ts`, `src/server/growth/store.ts`, and `tests/server/growthStore.test.ts` so `getGrowthProfile` applies the active account's saved Growth settings only when no `growth_profiles` row exists. Keep language, voice, audience, channels, hashtags, avoid text, posting windows, deterministic repository colour, and the sentinel `updatedAt` unchanged; copy settings values defensively and never rewrite an existing profile.
- Include active account ID in component load ownership, abort stale reads and writes on account change or unmount, reset form state before loading the next account, disable duplicate actions, and make save or reset affect the defaults returned by subsequent unpersisted profile reads without changing open or stored repository profiles optimistically.
- Update `src/styles/growth/library.css`, `src/i18n/en.ts`, and `src/i18n/it.ts`. Test keyboard editing, add and remove pillar controls, invalid timezone and weights, save and reset, account switching, stale cancellation, persisted-profile immutability, unpersisted-profile default application, AI link behavior, focus states, and dark and light layouts at 1440, 1024, and 390 pixels.

Acceptance:

- `/growth/settings` lets the active account manage reusable planning defaults, and only repositories without a saved profile inherit those settings while all existing repository-specific choices remain intact.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Record the final inheritance behavior in `docs/GS_DECISIONS.md`. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
