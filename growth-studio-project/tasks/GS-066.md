# GS-066 — Growth Studio release documentation and screenshots

**Phase:** Phase 6 — Unified and release

- Replace the Growth Studio screenshot placeholder and interim copy in `README.md` with the shipped repository-centric workflow: dedicated shell, Missions, Interventions and opportunity scans, editorial plans, month, week, Queue and unified calendars, mandatory media and browser-only image handling, manual publication recording, attribution, weekly Review, settings, and multi-repository planning. Keep documentation and image alternative text in English.
- Add deterministic, anonymized dark and light Growth Studio screenshots under `docs/images/` for Home, a repository calendar or Queue, and the unified calendar or global Review at desktop and mobile-representative sizes. Capture only fixture data with no token, private repository, account email, local filesystem path, or user-supplied secret, optimize files without adding an image dependency, and verify every README link and image path.
- Add an `Unreleased` Growth Studio section to `CHANGELOG.md` summarizing the dedicated `/growth` shell, additive SQLite model and legacy suggestion migration, editorial and media workflow, measure-and-learn loop, unified planning, and copy-paste-only publishing. Do not run a version bump or rewrite historical release entries.
- Document upgrade and operations behavior in `README.md`: automatic additive and idempotent schema initialization, one-shot legacy suggestion projection, data and private asset location under `~/.gitdeck/`, backup recommendation, authenticated asset serving, server-only forge and snapshot access, optional AI with deterministic fallback, no social credentials or outbound posting, and no new native image dependency. Run the standard gate plus a production-server deep-link and asset-path smoke test.

Acceptance:

- A new or upgrading user can understand, safely operate, and visually recognize the complete Growth Studio feature set from accurate release documentation with no leaked private data or broken assets.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
