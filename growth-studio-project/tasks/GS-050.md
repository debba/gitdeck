# GS-050 — Deterministic opportunity rules and scanning

**Phase:** Phase 5 — Measure and learn

- Create `src/utils/growth/opportunityRules.ts` and `tests/utils/growth/opportunityRules.test.ts` with pure, clock-injected detection for every D-045 opportunity: release follow-up, next star milestone, stale unanswered good-first issue, unshared large merged pull request, goal pace risk, and evergreen recycling. Use exact source URL matching against published content, stable rule keys, the 30-day activity lookback, three-day, 14-day, five-percent, five-percentage-point, 500-line, 20-file, and 60-day thresholds from D-045; malformed dates and incomplete signals must fail closed.
- Create `src/server/growth/rules.ts` and `tests/server/growthRules.test.ts`. Extend `src/types/growth.ts`, `src/server/growth/signals.ts`, `tests/server/growthSignals.test.ts`, `src/server/growth/store.ts`, and `tests/server/growthStore.test.ts` so repository signals include goal creation dates and at most 20 recently merged pull-request details, and rule interventions are upserted by account, repository, and stable `ruleKey`. Preserve every existing status and never alter non-rule rows; forge reads stay behind `restApi` and the server signal collector.
- Extend `src/server/routes/growth.ts`, `src/api/growth.ts`, and `tests/server/growthRoutes.test.ts` with allowlisted `POST /api/growth/interventions/scan` body `{ repository }` and a typed client helper. Validate the repository, require the active account, collect repository signals plus that account's content, persist only `origin='rule'` proposed interventions, and return the complete detected set with a scan timestamp. A cross-account row or repeated scan must not leak or duplicate work.
- Cover every positive and negative threshold boundary, stable ordering and keys, source-linked release and pull-request suppression, pace calculations, unavailable forge data, account scope, status preservation, and repeated scans. Add no UI or AI call in this task.

Acceptance:

- One explicit repository scan deterministically creates or refreshes each currently valid rule opportunity exactly once, preserves prior backlog decisions, and performs all forge access server-side.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
