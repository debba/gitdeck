# GS-046 — Media attachment workflow and ready gate

**Phase:** Phase 4 — Media

- Create `src/components/growth/GrowthMediaPicker.tsx`, `src/utils/growth/contentMedia.ts`, `tests/components/growth/GrowthMediaPicker.test.ts`, and `tests/utils/growth/contentMedia.test.ts`. Extend `src/components/growth/ContentItemDrawer.tsx`, `src/styles/growth/content-drawer.css`, `src/i18n/en.ts`, and `src/i18n/it.ts` with repository-library attachment and removal controls.
- Load assets with `fetchGrowthAssets` using the content item's account and repository identity, abort stale requests when either changes or the drawer closes, and render localized loading, empty, error, selected, and no-media states. Attach an asset with its `assetId`, kind, editable non-empty alt text, and optional caption; preserve legacy URL media, deduplicate selections, and use pure utility helpers for add/remove decisions.
- Persist every attachment or removal through the existing `patchGrowthContentItem` media field and replace drawer/Queue/calendar state from the response. Preview and image actions continue to use authenticated asset URLs from GS-040. Escape must still close the drawer, and all picker, remove, ready, copy, and download actions must be keyboard reachable at desktop and 390 pixels.
- Extend `src/server/routes/growth.ts`, `src/server/growth/store.ts`, `tests/server/growthStore.test.ts`, and `tests/server/growthRoutes.test.ts` so every supplied `assetId` must resolve to the active account and the content item's repository. Return a generic validation error for missing, cross-account, or cross-repository assets and never disclose whether another account owns an ID; existing valid HTTP/HTTPS URL media remains compatible.
- Complete gate coverage for create and patch to `ready`, scheduling, publication, removing the last attachment from `ready`, `scheduled`, or `published`, and valid media-preserving edits. Keep the store invariant that all three protected statuses require at least one resolvable attachment, disable ready when the current draft has none, and surface server media errors inline without optimistic removal.

Acceptance:

- A draft can attach and remove repository assets from the drawer, but no API or UI path can leave `ready`, `scheduled`, or `published` content without account-valid media; account boundaries, legacy URL media, keyboard behavior, and copy-paste-only publishing remain intact.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
