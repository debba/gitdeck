# GS-036 — Calendar ICS export

**Phase:** Phase 3 — Editorial plan

- Create `src/utils/growth/ics.ts`, `tests/utils/growth/ics.test.ts`, and endpoint coverage in `tests/server/growthRoutes.test.ts`. Extend `src/server/routes/growth.ts` with account-scoped `GET /api/growth/calendar.ics` and add a calendar URL builder to `src/api/growth.ts`.
- Accept allowlisted `repo`, `from`, and `to` query parameters. `repo` is optional for the already-settled future unified export; when supplied it must be a valid `owner/name`. Validate UTC ISO date-time bounds and reject an inverted range. Read only the active account's content through `listContentItems`.
- Export only `status='scheduled'` items with a valid `scheduledFor` in the requested range. Build RFC 5545 text in the pure utility with CRLF endings, escaped text, UTF-8-safe 75-octet line folding, stable `UID`, `DTSTAMP`, UTC `DTSTART`, summary, repository/channel/pillar description, and published/source URL when present. Return `text/calendar; charset=utf-8` and an attachment filename; do not expose account identifiers.
- Add an Export calendar link to `GrowthCalendar` for its repository and visible range, with matching English and Italian keys. The export remains a download only and does not write to an external calendar.

Acceptance:

- The endpoint downloads standards-compliant ICS for only the active account's scheduled items; malformed filters return 400; repository and date filters cannot leak other account data.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
