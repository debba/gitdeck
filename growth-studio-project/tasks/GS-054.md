# GS-054 — Repository and global weekly Growth Review

**Phase:** Phase 5 — Measure and learn

- Create `src/utils/growth/weeklyReview.ts` and `tests/utils/growth/weeklyReview.test.ts` with pure UTC ISO-week range building and the deterministic D-047 review model: published items, separate 48-hour and 7-day deltas, planned items missed by the end of the previous complete week, current-week upcoming work, channel and pillar findings, and exactly three bounded next-action recommendations. Inject the clock and produce useful empty-review copy data without inventing measurements.
- Create `src/server/growth/review.ts` and `tests/server/growthReview.test.ts`. Build repository or account-wide reviews from account-scoped content, interventions, and GS-053 summaries. Optionally call `generateStructured` once for narrative text only; unavailable AI, malformed output, and provider errors must all retain the deterministic narrative and core facts, with `aiEnabled` and `usedFallback` reported.
- Extend `src/types/growth.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with strict `GET /api/growth/review` and optional single `repo` filter. The read-only endpoint must validate scope and never persist recommendations, mutate content, refresh attribution, or make a forge request.
- Create `src/components/growth/GrowthReview.tsx`, `tests/components/growth/GrowthReview.test.ts`, and `src/styles/growth/review.css`; import the stylesheet from `src/styles.css` and replace both Review placeholders in `src/components/growth/GrowthStudioApp.tsx`. Add all strings to both locales and render loading, empty, error, deterministic-fallback, measured results, misses, upcoming items, three recommendations, and repository chips in global mode. Abort stale loads on account or repository changes and keep every item and link keyboard reachable at 1440, 1024, and 390 pixels.

Acceptance:

- `/growth/review` summarizes the latest complete week across the active account and each workspace Review shows the same model for one repository, with measured facts unchanged by optional AI and a deterministic result when AI fails or is disabled.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
