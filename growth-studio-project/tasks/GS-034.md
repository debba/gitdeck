# GS-034 — Calendar week view and rescheduling

**Phase:** Phase 3 — Editorial plan

- Create `src/components/growth/calendar/GrowthCalendarWeek.tsx` and extend `GrowthCalendar`, `src/utils/growth/calendar.ts`, `src/styles/growth/calendar.css`, `tests/components/growth/GrowthCalendar.test.ts`, and `tests/utils/growth/calendar.test.ts` with a seven-day week view selected by `?view=week&date=YYYY-MM-DD`.
- Add localized month/week controls and previous week, next week, and today navigation. Show profile posting hours as the grid guide while retaining all actual item times, and reuse the same focusable item and `ContentItemDrawer` behavior as month view.
- Implement dependency-free HTML drag and drop between day cells and an equivalent keyboard reschedule action. Convert the target local date back to UTC in the profile IANA timezone while preserving the item's local time. Persist with the existing `patchGrowthContentItem`, sending both `scheduledFor` and the current status so moving an `idea` or `draft` does not implicitly promote it to `scheduled` and bypass the media review.
- Apply optimistic movement only with rollback and an inline localized error when persistence fails. Reject drops outside the visible valid date cells. Tests cover DST-preserving movement, idea/draft status preservation, successful pointer and keyboard movement, rollback, and drawer interoperability.

Acceptance:

- Month and week views share one URL-addressable calendar; content can be moved by pointer or keyboard without changing its editorial status or weakening the mandatory-media rule.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
