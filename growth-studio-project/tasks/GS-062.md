# GS-062 — Multi-repository planning and pillar deconfliction

**Phase:** Phase 6 — Unified and release

- Create `src/utils/growth/planDeconfliction.ts` and `tests/utils/growth/planDeconfliction.test.ts` with deterministic deconfliction for repository-tagged output from `buildGrowthPlanSlots`. Preserve channel, format, pillar, local wall-clock time, slot counts, repository profile timezone, and the requested complete-week period; for a same-pillar local-date collision, move later stable repository and slot entries to the nearest free date in the period, preferring forward dates and leaving a collision only when no free date exists.
- Extend `src/server/growth/planner.ts`, `src/server/growth/store.ts`, and their existing tests with `generateMultipleGrowthContentPlans`. Accept two through ten distinct validated repositories and one shared one-to-four-week period, apply `getPerformanceAdjustedPillars` separately, build and deconflict all slots before assignment, keep one `generateStructured` assignment call per repository with the existing deterministic unavailable-AI fallback, and persist every plan and item in one transaction only after all repository preparation succeeds. Existing active-plan overlap, media, protected-content, profile-immutability, and single-repository generation behavior must remain unchanged.
- Extend `src/types/growth.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with allowlisted `POST /api/growth/plans/generate-multiple` body `{ repositories, periodStart, periodEnd }` and a typed result containing each existing `GrowthGeneratedContentPlanData` role plus a deconflicted-item count. Require the active account, reject duplicates, malformed ranges, unknown fields, and any overlap before persistence, and disclose no cross-account plan IDs.
- Add a multi-repository plan action to `src/components/growth/calendar/GrowthUnifiedCalendar.tsx` with localized repository selection, period, progress, fallback, re-weighting, partial-deconfliction, success, and error states in both locale files. Abort stale account requests, disable duplicate submissions, refresh the visible unified range after success, and test deterministic repository ordering, unavoidable collisions, DST edges, atomic failure, AI fallback and transport failure, account isolation, keyboard operation, and responsive selection at 390 pixels.

Acceptance:

- A maintainer can create compatible plans for several repositories in one explicit action, with same-pillar publication days staggered whenever the shared period permits and no partial generation on failure.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Record the final collision and atomicity rules in `docs/GS_DECISIONS.md`. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
