# GS-051 — Opportunity scanning in the Interventions backlog

**Phase:** Phase 5 — Measure and learn

- Extend `src/components/growth/GrowthInterventions.tsx`, `tests/components/growth/GrowthInterventions.test.ts`, `src/styles/growth/interventions.css`, `src/i18n/en.ts`, and `src/i18n/it.ts` with an explicit Scan opportunities action backed by `scanGrowthOpportunities` from GS-050. Keep the existing AI generation, manual form, filters, status groups, and drafting behavior unchanged.
- Include active account ID and repository in request ownership, abort an in-flight scan and backlog reload when either changes or the panel unmounts, disable duplicate actions while busy, and reconcile from the account-scoped server response rather than optimistic rows. A scan must never run automatically on navigation.
- Render localized initial guidance, scanning, no-op, created-or-refreshed summary, partial signal, and request-error states. Make rule origin and the six opportunity kinds understandable without exposing internal `ruleKey` values; retain category and origin filters and the collapsed dismissed group.
- Test keyboard activation, repeated scans, status preservation, empty and error states, account and repository cancellation, coexistence with AI generation, and responsive behavior at 390 pixels in dark and light themes.

Acceptance:

- A maintainer can explicitly scan one repository and immediately review deduplicated rule opportunities in the existing backlog without stale-account data or changes to AI and manual interventions.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
