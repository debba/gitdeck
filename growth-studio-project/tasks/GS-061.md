# GS-061 — Unified month, week, and filters UI

**Phase:** Phase 6 — Unified and release

- Create `src/components/growth/calendar/GrowthUnifiedCalendar.tsx` and `tests/components/growth/GrowthUnifiedCalendar.test.ts`, replace the `/growth/calendar` placeholder in `src/components/growth/GrowthStudioApp.tsx`, and consume `GET /api/growth/calendar` from GS-060. Keep `view=month|week` and `date=YYYY-MM-DD` in the URL, load only the rendered range, include active account ID in request ownership, abort stale loads, and render localized loading, empty, and error states.
- Extend `src/utils/growth/unifiedCalendar.ts`, `tests/utils/growth/unifiedCalendar.test.ts`, and the existing components under `src/components/growth/calendar/` rather than copying month, week, item, drawer, date-grid, or rescheduling logic. Group each item on its repository-profile local date, show the repository chip and profile colour, format its time in that profile timezone, and preserve the existing native drag plus `Alt+ArrowLeft` or `Alt+ArrowRight` status-preserving reschedule behavior. The repository route `/growth/r/:owner/:repo/calendar` and Queue remain unchanged.
- Add URL-backed repository, channel, pillar, and status filters with explicit All values. Filter the loaded account-scoped model client-side, keep choices stable for the visible range, expose a clear-filters action, open `ContentItemDrawer` from every item, and use the existing account-wide `buildGrowthCalendarExportUrl` without a repository filter for the visible-range ICS download.
- Update `src/styles/growth/calendar.css`, `src/i18n/en.ts`, and `src/i18n/it.ts`. Test filter combinations and URL restoration, repository colours, equal pillar IDs across repositories, timezone date edges, pointer and keyboard moves, drawer Escape, account switching, request failure, focusability, no page-level overflow, and dark and light layouts at 1440, 1024, and 390 pixels.

Acceptance:

- `/growth/calendar` presents one filterable, colour-coded month or week across the active account while retaining the repository calendars' editing, keyboard, timezone, Queue, and routing contracts.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
