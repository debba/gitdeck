# GS-037 — Editorial plan management

**Phase:** Phase 3 — Editorial plan

- Create `src/components/growth/calendar/GrowthPlanManager.tsx` and `tests/components/growth/GrowthPlanManager.test.ts`. Integrate it above the repository `GrowthCalendar`, add styles to `src/styles/growth/calendar.css`, and add every string to `src/i18n/en.ts` and `src/i18n/it.ts`.
- Extend `src/server/growth/planner.ts`, `src/server/growth/store.ts`, `src/server/routes/growth.ts`, `src/api/growth.ts`, `src/types/growth.ts`, `tests/server/growthPlanner.test.ts`, and `tests/server/growthRoutes.test.ts` with account-scoped regeneration and archive operations: `POST /api/growth/plans/:id/regenerate` and `POST /api/growth/plans/:id/archive`. Both reject unknown bodies and cross-account IDs with 404.
- Regeneration uses the source plan's repository and inclusive period with the current profile and signal evidence. Build and normalize the replacement before one transaction archives the source, marks only its `idea` and `draft` items `skipped`, and creates the replacement active plan and ideas. A generation failure leaves the source plan untouched. Preserve ready, scheduled, published, and already skipped items.
- Archiving marks the plan archived and marks only its remaining idea/draft items skipped in one transaction. Never delete reviewed or published content. Return the updated source plan and affected items so calendar state can refresh.
- The manager lists plans from `fetchGrowthContentPlans`, defaults creation to the next complete ISO week, allows one to four complete weeks, calls the existing generate endpoint, and offers regenerate/archive with confirmations. Disable duplicate actions while busy and render localized loading, empty, AI-fallback, validation, and request-error states. Successful operations refresh both plans and visible calendar items.

Acceptance:

- Users can create, regenerate, and archive repository plans; overlap and account checks hold; failed regeneration is atomic; reviewed content survives plan lifecycle actions.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
