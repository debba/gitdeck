# GS-067 — Final Growth Studio release QA

**Phase:** Phase 6 — Unified and release

- Audit every Growth Studio feature row in `docs/GS_FEATURE_MATRIX.md` against the shipped locations and mark the complete release surface `DONE`; fix only release-blocking Growth Studio regressions. Exercise `/growth`, global calendar and Review, settings, every repository workspace panel, legacy `/goals` redirect, and dashboard routes at 1440, 1024, and 390 pixels in dark and light themes, including loading, empty, error, and populated states with no page-level overflow or dashboard chrome leakage.
- Verify keyboard and accessibility behavior: logical tab order, visible focus, semantic labels, calendar item activation, `Alt+ArrowLeft` and `Alt+ArrowRight` moves, drag alternatives, Escape dismissal for sidebar, drawers, and dialogs, non-colour status cues, repository chips, media alternative text, and screen-reader status or alert announcements. Confirm English and Italian key parity and inspect both locales for clipped or untranslated controls.
- Run security and data-integrity QA with isolated accounts and temporary data: anonymous and cross-account 401 or 404 behavior, strict unknown and duplicate input rejection, server-only forge and snapshot reads, SSRF and redirect guards, bounded uploads and proxies, authenticated asset paths, SVG escaping, browser-only rasterization, mandatory media statuses, copy-paste-only publishing, status preservation, published-source immutability, profile and settings inheritance, and multi-plan atomicity. Add focused tests under mirrored `tests/utils`, `tests/server`, or `tests/components` locations for any repaired regression.
- Exercise upgrades from an existing pre-Growth database and from legacy `repository_goals.suggestions` data twice, proving additive idempotent schema setup, one-shot account-scoped migration, retained goals and sources, private assets, performance cascade behavior, and settings defaults. Run `growth-studio-project/scripts/validate-gs-task.sh` without relying on a stale cache, production build and server deep-link smoke tests, `git diff --check`, dependency and outbound-posting audits, and confirm README screenshots and CHANGELOG are release-ready.

Acceptance:

- Growth Studio is release-ready across accounts, routes, themes, breakpoints, keyboard workflows, security boundaries, migrations, deterministic fallbacks, documentation, and production serving without changing unrelated GitDeck behavior.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update all final `docs/GS_FEATURE_MATRIX.md` rows and append any release decision to `docs/GS_DECISIONS.md`. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
