# GS-065 — Global home and review release integration

**Phase:** Phase 6 — Unified and release

- Extend `GrowthWorkspaceSummary` in `src/types/growth.ts` and `getGrowthWorkspaceSummary` in `src/server/growth/store.ts` with the repository's effective profile colour, preserving the existing `/api/growth/workspaces` and `fetchGrowthWorkspaces` envelope. Cover the additive field in `tests/server/growthStore.test.ts`, `tests/server/growthRoutes.test.ts`, and `tests/api/growth.test.ts`, including profile-only, goal-only, default-colour, and account-isolation cases.
- Extend `src/utils/growthHome.ts`, `tests/utils/growthHome.test.ts`, and `src/components/growth/GrowthHome.tsx`, and create `tests/components/growth/GrowthHome.test.ts` to retain the full workspace summaries instead of only repository names. Add deterministic account totals for proposed and accepted interventions, draft and ready content, and the next seven days; show profile-coloured repository identity, preserve fallback repository identities, and add prominent links to the unified calendar, global Review, and Growth settings.
- Extend `src/components/growth/GrowthReview.tsx` and `tests/components/growth/GrowthReview.test.ts` with an explicit Refresh measurements action that calls the existing `refreshGrowthContentPerformance` for the current repository or active account and then reloads the read-only `fetchGrowthReview` result. Report refreshed and pending 48-hour or 7-day windows without estimating deltas, keep optional AI limited to narrative, and do not make Review navigation trigger snapshot or forge work automatically.
- Reuse `GrowthWorkspaceSummary`, `GrowthContentPerformanceRefreshData`, `GrowthReview`, and existing API helpers rather than adding endpoints. Include active account ID and repository in request ownership, abort stale home, refresh, and review requests, disable duplicate actions, preserve the current review on refresh failure, and reconcile only server-returned data.
- Update the focused Growth home and review styles plus `src/i18n/en.ts` and `src/i18n/it.ts`. Test empty and populated global totals, profile-only and goal-only workspaces, pending attribution reasons, successful and failed refresh, repository versus account scope, AI-disabled fallback, keyboard links and actions, account switching, and dark and light layouts at 1440, 1024, and 390 pixels.

Acceptance:

- Growth Home exposes the account-wide workflow at a glance, and repository or global Review can explicitly refresh real measurements before presenting the unchanged deterministic learning model.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
