# GS-043 — Generated SVG cards

**Phase:** Phase 4 — Media

- Create `src/server/growth/cards.ts` and `tests/server/growthCards.test.ts`. Extend `src/types/growth.ts`, `src/server/growth/assets.ts`, `src/server/growth/store.ts`, `src/server/routes/growth.ts`, `src/api/growth.ts`, `src/components/growth/GrowthAssetLibrary.tsx`, `tests/server/growthRoutes.test.ts`, and `tests/components/growth/GrowthAssetLibrary.test.ts` with generated-card assets.
- Define shared card template identifiers and validated data shapes for `release`, `milestone`, `stats`, `quote`, and `whats-new`. Render deterministic 1200 by 675 SVG strings using the repository profile colour, repository identity, and bounded template data; XML-escape every user value, use no remote fonts or embedded scripts, and include an accessible title and description.
- Add JSON `POST /api/growth/assets/cards` with allowlisted `{ repository, template, title, alt, data }`. Validate the repository, template-specific fields, lengths, arrays, numbers, and account scope; persist one `growth_assets` row with `kind='image'`, `origin='generated'`, `cardTemplate`, and normalized `cardData`, but no path, URL, or raster bytes.
- Extend authenticated `GET /api/growth/assets/:id/file` from GS-040 to render generated card metadata as `image/svg+xml` with private no-store and nosniff headers. The server must never rasterize the SVG and malformed stored metadata must fail safely without returning executable markup.
- Add a keyboard-accessible card creator to `GrowthAssetLibrary` with template-specific fields, localized validation/errors, preview through the authenticated asset URL, and list refresh after creation. Tests cover every template, deterministic output, XML injection, bounds, malformed data, cross-account reads, persistence round trips, and Library creation states.

Acceptance:

- All five templates create reusable account-scoped image assets and safe deterministic SVG previews; no server rasterization, scriptable user markup, filesystem path, outbound posting, or native image dependency is introduced.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
