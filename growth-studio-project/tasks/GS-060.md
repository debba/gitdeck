# GS-060 — Unified calendar read model and API

**Phase:** Phase 6 — Unified and release

- Create `src/utils/growth/unifiedCalendar.ts` and `tests/utils/growth/unifiedCalendar.test.ts` with a pure, stable read model that combines account-owned dated `GrowthContentItem` rows with repository calendar metadata. Include repository name, `GrowthProfile.color`, timezone, posting windows, and pillar labels; retain all editorial statuses except `skipped`, derive defaults through `createDefaultGrowthProfile` when an item has no persisted profile, and order repositories and items deterministically without merging repository-local dates.
- Create `src/server/growth/calendar.ts` and `tests/server/growthCalendar.test.ts`. Reuse `listContentItems`, `listPersistedGrowthProfileRepositories`, and `getGrowthProfile`; perform no forge, snapshot, AI, or outbound publishing work. Return every persisted-profile repository plus metadata for repositories represented by visible account-owned items, while keeping profiles and content from other accounts unavailable.
- Extend `src/types/growth.ts`, `src/server/routes/growth.ts`, `tests/server/growthRoutes.test.ts`, and `src/api/growth.ts` with typed `GrowthUnifiedCalendarData` transport and strict `GET /api/growth/calendar?scheduledFrom=<UTC ISO>&scheduledTo=<UTC ISO>`. Require both inclusive range bounds, reject duplicate, unknown, malformed, or reversed filters, and use the existing `{ ok: true, ... }` and 401 error contracts.
- Test empty and mixed-repository calendars, repositories with profile-only and content-only state, profile colours and timezone metadata, every included status, skipped and undated exclusion, boundary inclusion, deterministic ordering, account isolation, stale client cancellation, malformed queries, and confirmation that the existing `/api/growth/calendar.ics` contract is unchanged.

Acceptance:

- One bounded account-scoped request supplies the complete read-only model needed by a unified calendar, including private repository presentation metadata, without changing per-repository calendar or ICS behavior.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
