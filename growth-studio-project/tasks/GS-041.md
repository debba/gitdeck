# GS-041 — Library asset uploads

**Phase:** Phase 4 — Media

- Create `src/components/growth/GrowthAssetLibrary.tsx` and `tests/components/growth/GrowthAssetLibrary.test.ts`. Integrate it into `src/components/growth/GrowthLibrary.tsx`; extend `src/styles/growth/library.css`, `src/i18n/en.ts`, and `src/i18n/it.ts` without changing the existing profile and source editors.
- Load assets with `fetchGrowthAssets` for the active account and fixed repository, include account ID and repository in effect dependencies, abort stale list and upload requests on switch or unmount, and refresh the list after upload. Render localized loading, empty, validation, upload-progress, and request-error states.
- Add a keyboard-reachable image/video upload form with required title and alt text. Accept only the MIME types and 25 MiB limit established by GS-040, obtain image dimensions in the browser when available, call `uploadGrowthAsset`, and reset the form only after success; do not parse or transform image bytes on the server.
- Render a responsive asset grid with origin, kind, dimensions, title, alt text, and previews from `buildGrowthAssetFileUrl`. Use native lazy image/video loading, preserve aspect ratios, and keep the authenticated file route as the only source for uploaded bytes. Asset deletion and source imports remain outside this task.
- Tests cover account/repository reload and abort behavior, empty/error states, client validation, successful upload and refresh, accessible labels and controls, and authenticated preview URLs at desktop and 390-pixel layouts.

Acceptance:

- The repository Library lists its account-scoped assets and uploads supported image or video files with useful alt text, without stale account data, public file paths, native dependencies, or regressions to profile/source editing.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
