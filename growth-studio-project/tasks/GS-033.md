# GS-033 — Repository calendar month view

**Phase:** Phase 3 — Editorial plan

- Create `src/components/growth/calendar/GrowthCalendar.tsx`, `src/components/growth/calendar/GrowthCalendarMonth.tsx`, `src/utils/growth/calendar.ts`, `src/styles/growth/calendar.css`, `tests/components/growth/GrowthCalendar.test.ts`, and `tests/utils/growth/calendar.test.ts`. Import the stylesheet from `src/styles.css`.
- Replace the `/growth/r/:owner/:repo/calendar` placeholder in `src/components/growth/GrowthStudioApp.tsx` with `GrowthCalendar`, keyed by active account and repository. Load the profile with `fetchGrowthProfile` and the visible grid's items with `fetchGrowthContentItems`; include the active account ID in dependencies and abort stale requests.
- Build a Monday-first six-week month grid in the pure calendar utility and group `scheduledFor` values by local date in the profile IANA timezone. Keep month selection in the URL as `?view=month&date=YYYY-MM-DD`, normalize invalid dates, and provide previous month, next month, and today controls.
- Render every non-skipped item with a scheduled time in the visible grid, with channel, status, and pillar indicators. Items are focusable buttons and open the existing `ContentItemDrawer`; drawer updates replace the item in calendar state. Add localized loading, empty, and error states to both locale files and styles for dark, light, desktop, and 390-pixel layouts.

Acceptance:

- The repository calendar deep-links to a stable month, groups dates in the profile timezone, opens the reusable drawer by keyboard or pointer, refreshes an edited item, and does not show stale data after an account switch.

## Completion record

Before finishing, update the matching row in `growth-studio-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the ISO date. Do not use `|` inside progress fields. Update `docs/GS_FEATURE_MATRIX.md` rows touched by this task. Create the task's single conventional commit (scope `growth`, no `Co-Authored-By` trailer) including the PROGRESS.md update. Do not push.
